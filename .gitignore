# Node.js 관련
node_modules/             # Node.js 종속성 모듈
.env                     # 환경 변수 파일
.env.local               # 로컬 환경 변수 파일
.env.production          # 프로덕션 환경 변수 파일
.env.*.local             # 기타 로컬 환경 변수 파일들

# Vite (React) 관련
client/node_modules/      # Vite React 종속성 모듈
client/dist              # Vite 빌드 출력 폴더
client/dist-ssr          # Vite 서버 측 빌드 폴더
client/.env              # React 환경 변수 파일
client/.env.local        # React 로컬 환경 변수 파일
client/.env.production   # React 프로덕션 환경 변수 파일
client/.env.*.local      # 기타 로컬 환경 변수 파일들

# Logs
logs/                    # 로그 폴더
*.log                    # 모든 로그 파일
npm-debug.log*           # npm 디버그 로그 파일들
yarn-debug.log*          # yarn 디버그 로그 파일들
yarn-error.log*          # yarn 에러 로그 파일들
pnpm-debug.log*          # pnpm 디버그 로그 파일들
lerna-debug.log*         # Lerna 디버그 로그 파일들

# Build output directories
dist/                    # 최상위 빌드 폴더
dist-ssr/                # 서버 측 렌더링 빌드 폴더
*.local                  # 로컬 빌드 환경 파일

# Editor directories and files
.vscode/*                # VS Code 설정 폴더
!.vscode/extensions.json # VS Code 확장 설정 파일은 제외하지 않음
.idea                    # WebStorm, IntelliJ 설정 폴더
.DS_Store                # macOS 시스템 파일
*.suo                    # Visual Studio 파일
*.ntvs*                  # Visual Studio 파일
*.njsproj                # Node.js 프로젝트 파일 (Visual Studio)
*.sln                    # Visual Studio 솔루션 파일
*.sw?                    # vim 스왑 파일