import styled from '@emotion/styled';

import visialImage from '@assets/images/visual_quiz_imagex2.png';

export const StyledThumnail = styled('div')`
  position: relative;
  background: url(${visialImage}) center / cover no-repeat;
`;
